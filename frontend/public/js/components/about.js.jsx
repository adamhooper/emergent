/** @jsx React.DOM */

var React = require('react');

module.exports = React.createClass({
  render: function() {
    return (

      /* JSX for about page */
      <div className="container">
        <h1>About Emergent</h1>
        <p>Updates and analysis from Emergent.info, a real-time rumor tracker.</p>
      </div>

    );
  }
});